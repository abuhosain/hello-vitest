{
  "name": "hello-vitest",
  "type": "module",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "vitest --reporter verbose --run --environment jsdom"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
